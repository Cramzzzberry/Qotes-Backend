{
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.0.1",
    "socket.io": "^4.7.2"
  },
  "name": "quotesapp",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve:dev": "set NODE_ENV=development && dotenv -e .env.development nodemon app.js",
    "serve:prod": "set NODE_ENV=production && dotenv -e .env.production nodemon app.js",
    "start": "set NODE_ENV=production && dotenv -e .env.production nodemon app.js",
    "migrate:dev": "dotenv -e .env.development npx prisma migrate dev",
    "migrate:prod": "dotenv -e .env.production npx prisma migrate dev",
    "generate": "npx prisma generate",
    "studio": "dotenv -e .env.development npx prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prisma": "^5.6.0"
  }
}
